# Implementation Plans

Generated by the improve skill on 2026-08-21, from a vetted audit of
`architecture_refactor` at commit `800db1c` (finding selection by the
maintainer). Execute in the order below unless dependencies say otherwise.
Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

House rules that bind every plan here (from `CLAUDE.md`):

- `resources/roadmap/pypts_roadmap.md` is the single source of truth for
  status — every plan's final steps include its roadmap entry, made **in the
  same change** that implements it.
- `src/pypts/old_code/` is never modified. The three quality gates
  (`pytest tests`, `ruff check src tests`, `mypy`) must pass before any change
  is called done. At `800db1c` ruff carries 6 pre-existing findings
  (3 × `logger/log.py`, `report/report.py:10` I001, `sequencer/sequencer.py:146`
  E501, `:213` BLE001) — "pass" for these plans means *no new findings*;
  plan 001 additionally clears the `report.py` one.
- Machine quirk: if pytest fails with `PermissionError ... pytest-of-Dzbanan`,
  the machine's pytest temp root is broken — add
  `--basetemp=C:\Git\pts-framework\.pytest-tmp` (disposable, never commit it).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Report run-state hardening (failed run folder can no longer resurrect the previous run) | P1 | S | — | DONE (reviewed; `9a9edad` on `exec/001-report-run-state`) |
| 002 | Shutdown choreography (StopReport held for the Sequencer; abandoned sequence reported CRITICAL) | P1 | M | — | DONE (reviewed; `8ae180d` on `exec/002-shutdown`) |
| 003 | Test net under shutdown / abort / exit-handshake paths | P2 | M | 002 | DONE (reviewed; `9104e1f` on `exec/003-test-net`) |
| 004 | Unreadable config.ini reported as unreadable, not as a version mismatch | P2 | S | — | DONE (reviewed; `5d6a13e` on `exec/004-config-unreadable`) |
| 005 | Launcher pins multiprocessing to "spawn" on every platform | P2 | S | — | DONE (reviewed; `8e94168` on `exec/005-spawn`) |

All five executed serially in stacked worktrees under `.claude/worktrees/exec-00N`;
each branch builds on the previous, so **`exec/005-spawn` (`8e94168`) contains the
complete work** and is a direct descendant of `architecture_refactor`'s `800db1c` —
merging it is a fast-forward, and it is the maintainer's call. Final gates on that
branch, verified by the reviewer: 408 passed / 43 skipped, ruff = the 5 pre-existing
findings (report.py's I001 was cleared by 001), plain `mypy` clean. Owed: one Linux
end-to-end run for 005 (recorded in the roadmap bullet).

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **003 requires 002**: its Core tests assert 002's new contract (`StopReport`
  held until `SequencerStopped` or the shutdown deadline). Run before 002 they
  fail by design.
- 001, 004, 005 are independent of everything and of each other.
- 001 and 002 both touch the Report's lifecycle but different halves
  (001: `start_run`; 002: *when* `StopReport` arrives) — either order works.

## Findings considered and rejected / deferred

Recorded so nobody re-audits them. "Deferred" = real, maintainer chose not to
plan it now (2026-08-21); "rejected" = vetting killed it.

**Rejected in vetting:**

- *"SSH password duplicated in `recipe_guide.md:749`"* — false: exact-match
  search finds the literal only in `resources/recipes/comprehensive_recipe.yml`
  (the known F22). Addition to F22 recorded: line **21** of that recipe also
  carries a short non-path `private_key` value — rotation should cover
  lines 19–21, not just 20.
- *"PythonModuleStep re-imports the module per step"* — already tracked as
  R-2 in `resources/roadmap/porting_changes.md`; not re-planned here.

**Deferred by maintainer decision:**

- **Functional-test first slice + `old_code` characterization baseline** (all
  20 `tests/functional_tests/` tests are skipped placeholders; no CSV golden
  file pins the port). Highest-leverage tests finding of the audit; revisit
  when the recipe/engine surface settles.
- **Docs purge** (`docs/source/yaml_format.rst`, `usage.rst`, `api.rst`,
  `troubleshooting.rst`, `report_generation.rst`, `gui_event_handling.rst`,
  `README.md` + `index.rst` all describe the pre-refactor engine; `api.rst`
  autodocs four dead modules so the published API page is empty). Deferred:
  "the whole recipe needs refactoring" — rewrite when the format lands.
- **Quality-gate repair** (ruff currently red beyond the known 3; CI `--cov`
  uses undeclared `pytest-cov`, measures frozen code, no threshold; REUSE job
  commented out with drifted `reuse.toml` globs; no pytest-timeout; no
  Windows CI job). "No P6 at the moment."
- **Dependency extras split** — seven of eleven core deps (`nidmm`,
  `hightime`, `nptdms`, `paramiko`, `matplotlib`, `pyserial`, `numpy`) have
  zero import sites in the shipped package; plus inconsistent pinning and the
  `ruamel.yaml`-only-for-the-creator question. No benches exist yet, so the
  move is currently free — cheap first slice of the roadmap's Phase-5 split
  whenever wanted.
- **Monotonic clocks for liveness/shutdown** — heartbeat aging, the shutdown
  deadline and `wait_until_stopped` all use `time.time()`; an NTP step can
  declare modules dead or stretch budgets (`core.py:544,569,600`,
  `heartbeat_manager.py:67`, `hmi_client.py:192`). S-effort, worth doing
  before real benches.
- **Redaction seam for step inputs** — resolved inputs go verbatim to the run
  log at INFO (`steps.py:147`), to `report.csv`/`report.html`, and twice to
  the DEBUG trace; the config DEBUG dump also logs unknown-section values
  (`config_handler.py:267-276`). Must be designed **before** the SSH-step
  port / Phase-5 device credentials — treat as that port's precondition.
- **HMI error echo loop** — Core echoes an HMI-originated `ModuleError` back
  as `ModuleErrorReported` (`core.py:346-349,510-511`); a frontend whose
  `show_error` fails can loop. S-effort guard when touched next.
- **Skip-inventory + agent-docs refresh** — 5 of 45 skips are
  stale/contradictory (`test_utilities.py:334,339`,
  `test_hmi_cli.py:57,62,67`); `CLAUDE.md` still says the engine doesn't
  exist and pins the 341/51 baseline (now 388/45); `messages.md` intro
  contradicts its own body; roadmap still lists the done `requirements.txt`
  item; `pyproject.toml:23` gives Bartlomiej Banas a copy-pasted
  `joseph.tagg@cern.ch` email.
- **`skip:`/`critical:` recipe keys** — `skip:` works but is documented
  nowhere current; `critical:` is accepted and inert (the third member of the
  M-6/M-7 silent-ignore family, previously untracked). Decide with the
  continue-on-error policy design.
- **Debug Monitor robustness trio** — `parse_line` can raise despite its
  "never raises" contract; newline-bearing log messages parse as forged
  records; Core's two liveness sentences are string-matched with no shared
  constant (`liveness.py:77-78` vs `core.py:521,549`). Tooling-only.
- **Dead-but-maintained surface** — config write API
  (`set_parameter`/`restore_default`/`open_for_writing`: zero production
  callers), `report_and_reraise` (users arrive with the step ports),
  `convert_string_to_int`; six scattered poll-interval literals; the Debug
  Monitor started (with its up-to-5 s log-file wait) before
  `core_process.start()`.
- **Direction items** — D1: reconcile roadmap §3 (plugin discovery written
  for the process model) with §1.5's thread topology before Phase 2; D2: a
  read-only `--dump-config` surface (the API exists, untouched); D3 folded
  into the `critical:` item above.
- **Typing nit found during execution** (2026-08-21): `config_handler.py`
  `__new__` is annotated `-> Self` but `_obtain()` returns `"ConfigHandler"` —
  invisible to the plain `mypy` gate (config_handler is outside `[tool.mypy]
  files`), but it surfaces the day the scope widens. Two executors tripped
  over it with wider invocations. One-line fix whenever mypy's scope grows.
