[OperatingSystem]
# Information about the OS running the application
name = Unknown
version = Unknown
architecture = Unknown
kernel = Unknown

# Note: paths (temp/log/config dirs) are derived at runtime from
# tempfile.gettempdir() by pypts.utilities.local_storage, so they are
# cross-platform correct and are not stored here.

[Application]
# Basic application settings
log_level = DEBUG
app_version = 1.0.0

[Misc]
# Miscellaneous settings, placeholder for future configs
example_flag = True
